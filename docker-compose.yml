services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: solva
      POSTGRES_PASSWORD: solva
      POSTGRES_DB: solva_hris
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
